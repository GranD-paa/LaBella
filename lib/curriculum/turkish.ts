import type { CurriculumLevel } from "@/lib/curriculum/types";

// Turkish starts with an empty curriculum. Super admins build levels from
// the Language Management page (A1-1, A1-2, …) independently of Italian.
export const TURKISH_LEVELS: CurriculumLevel[] = [];
