# The locale JSON is generated by `npm run messages:export`, and a test
# compares the file on disk against the generator's output byte for byte. The
# generator writes LF. Without this line a Windows checkout rewrites the file
# with CRLF, and the test fails on a tree nobody has touched — which is what
# happens every time the branch changes.
locales/*/translation.json text eol=lf
